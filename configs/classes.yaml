# Object classes for YOLO detection - Robot Arm Challenge
# Format: id: name
classes:
  0: nut
  1: bolt
  2: screw

# Detection thresholds per class (optional overrides)
thresholds:
  default: 0.5
  nut: 0.55
  bolt: 0.55
  screw: 0.55
